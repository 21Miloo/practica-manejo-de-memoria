# Binarios del juego
memlab
memlab_pruebas

# Salidas de compilacion
build/
*.o
*.out

# Progreso local de las partidas
.memlab_progreso

# El modulo WebAssembly compilado SI se versiona: asi se puede jugar la version
# web clonando el repositorio, sin instalar el SDK de Emscripten.
# (para regenerarlo: ./web/construir.sh)

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# Paso intermedio del empaquetado (el resultado es web/memlab.html)
web/wasm/memlab_unico.js
